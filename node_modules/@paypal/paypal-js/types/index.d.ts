import type { PayPalScriptOptions } from "@paypal/paypal-js/types/script-options";
import type {
    PayPalButtonsComponentOptions,
    PayPalButtonsComponent,
} from "@paypal/paypal-js/types/components/buttons";
import type {
    PayPalMarksComponentOptions,
    PayPalMarksComponent,
} from "@paypal/paypal-js/types/components/marks";
import type {
    PayPalMessagesComponentOptions,
    PayPalMessagesComponent,
} from "@paypal/paypal-js/types/components/messages";
import type {
    FUNDING_SOURCE,
    getFundingSources,
    isFundingEligible,
    rememberFunding,
} from "@paypal/paypal-js/types/components/funding-eligibility";
import type { PayPalHostedFieldsComponent } from "@paypal/paypal-js/types/components/hosted-fields";
import type {
    PayPalCardFieldsComponentOptions,
    PayPalCardFieldsComponent,
} from "@paypal/paypal-js/types/components/card-fields";

export interface PayPalNamespace {
    Buttons?: (
        options?: PayPalButtonsComponentOptions,
    ) => PayPalButtonsComponent;
    Marks?: (options?: PayPalMarksComponentOptions) => PayPalMarksComponent;
    Messages?: (
        options?: PayPalMessagesComponentOptions,
    ) => PayPalMessagesComponent;
    HostedFields?: PayPalHostedFieldsComponent;
    CardFields?: (
        options?: PayPalCardFieldsComponentOptions,
    ) => PayPalCardFieldsComponent;
    getFundingSources?: getFundingSources;
    isFundingEligible?: isFundingEligible;
    rememberFunding?: rememberFunding;
    FUNDING?: Record<string, FUNDING_SOURCE>;
    version: string;
}

export function loadScript(
    options: PayPalScriptOptions,
    PromisePonyfill?: PromiseConstructor,
): Promise<PayPalNamespace | null>;

export function loadCustomScript(options: {
    url: string;
    attributes?: Record<string, string>;
    PromisePonyfill?: PromiseConstructor;
}): Promise<void>;

export const version: string;

declare global {
    interface Window {
        paypal?: PayPalNamespace | null;
    }
}

// Export components
export * from "@paypal/paypal-js/types/components/buttons";
export * from "@paypal/paypal-js/types/components/funding-eligibility";
export * from "@paypal/paypal-js/types/components/hosted-fields";
export * from "@paypal/paypal-js/types/components/marks";
export * from "@paypal/paypal-js/types/components/messages";
export * from "@paypal/paypal-js/types/components/card-fields"

// Export apis
export * from "@paypal/paypal-js/types/apis/orders";
export * from "@paypal/paypal-js/types/apis/subscriptions";

// Export script-options
export * from "@paypal/paypal-js/types/script-options";
